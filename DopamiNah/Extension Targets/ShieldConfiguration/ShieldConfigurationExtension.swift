// Mock extension - stub implementation for main target compilation
// Real ShieldConfiguration logic lives in the separate extension target

import SwiftUI

struct ShieldConfigurationExtensionStub: View {
    var body: some View {
        Text("Shield Configuration")
    }
}
